import { useLocale } from "next-intl";
import React from "react";

const LeagalFooterText = () => {
  const locale = useLocale();
  return <div>LeagalFooterText</div>;
};

export default LeagalFooterText;
